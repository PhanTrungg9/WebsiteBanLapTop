@{
    ViewBag.Title = "Them";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@section naviheader{
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/category" class="nav-link">Thương hiệu</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="#" class="nav-link">Thêm mới</a>
        </li>
    </ul>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Thêm mới thương hiệu</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Thương hiệu</a></li>
                    <li class="breadcrumb-item active">Thêm mới</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<section class="content">
    <div class="card">
        <div class="card-body">
            <div class="form-group">
                <label>Mã thương hiệu </label>
                <input type="text" class="form-control" id="txt_brandid" name="txt_brandid" />
            </div>

            <div class="form-group">
                <label>Tên thương hiệu</label>
                <input type="text" class="form-control" id="txt_name" name="txt_name" />
            </div>

            <div class="form-group">
                <label>Mô tả</label>
                <input type="text" class="form-control" id="txt_description" name="txt_description" />
            </div>

            <div class="form-group">
                <a href="/Admin/QLTH/DanhSachThuongHieu" class="btn btn-danger">Quay lại</a>
                <button type="button" id="btn_them" class="btn btn-primary">Thêm</button>
            </div>
        </div>
    </div>
</section>

@section scripts{
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $("#btn_them").click(function () {

            // Lấy dữ liệu trực tiếp từ input
            var data = {
                txt_brandid: $("#txt_brandid").val(),
                txt_name: $("#txt_name").val(),
                txt_description: $("#txt_description").val()
            };

            $.ajax({
                type: "POST",
                url: "/Admin/QLTH/Insert",
                data: data,
                success: function (response) {
                    alert(response); // Hiện thông báo từ Controller
                    // Nếu muốn quay lại danh sách sau khi thêm thành công
                    if (response.includes("thành công")) {
                        window.location.href = "/Admin/QLTH/DanhSachThuongHieu";
                    }
                },
                error: function () {
                    alert("Có lỗi xảy ra khi thêm thương hiệu!");
                }
            });
        });
    </script>
}
